<!-- /templates/page.liquid -->
<h1 class="small--text-center">{{ page.title }}</h1>

<div class="rte">
  {% if page.content != blank %}
    {{ page.content }}
  {% else %}
    {% section 'image-customizer-tool' %}
  {% endif %}
</div>
